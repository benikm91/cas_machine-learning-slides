ng-template('#slides'='')
    section(id="{{problemTableOfContentLabel.SUPERVISED_PROBLEM}}")
        slide-with-header(header="{{problemTableOfContentLabel.SUPERVISED_PROBLEM}}")
            div
                div
                    span
                        | Daten unterteilt in #[span.fragment.highlight-red Input und dazugehörende Output] (unabhängige und abhängige Variablen)
            div.fragment.fade-up('#parent'='').pt-5.d-flex.justify-content-center
                ng-container(*ngIf="parent.className.includes('visible')")
                    supervised-model-visualization

