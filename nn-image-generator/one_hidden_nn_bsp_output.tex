\documentclass[preview]{standalone}
\usetikzlibrary{positioning}

\input{neural_network_commands.tex}

\begin{document}

%%% one_hidden_nn_bsp_output.png

\begin{preview}
    \begin{tikzpicture}[x=1.5cm, y=1.5cm, >=stealth]
    \inputs{{1,2,missing,p}}
    \hidden{{1,missing,h}}{$\sum \rightarrow \phi$}
    \outputs{{1,missing,o}}{$\sum \rightarrow \phi$}
    \layers
    \inputhiddenconnections{{bias,1,2,p}}{{1,h}}

    \foreach \i in {bias}
    \foreach \j in {1}
    \draw [->,red] (hidden-\i) -- (output-\j) node[pos=0.25,left,above] {$\beta^{(2)}_{\j,0}$};

    \foreach \i in {1,h}
    \foreach \j in {1}
    \draw [->,red] (hidden-\i) -- (output-\j) node[pos=0.25,left,above] {$\beta^{(2)}_{\j,\i}$};

    \foreach \i in {bias,1,h}
    \foreach \j in {o}
    \draw [->] (hidden-\i) -- (output-\j) node[pos=0.25,left,above] {};

    \end{tikzpicture}
\end{preview}

\end{document}