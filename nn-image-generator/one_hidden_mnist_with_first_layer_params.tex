\documentclass[preview]{standalone}
\usetikzlibrary{positioning}

\input{neural_network_commands.tex}

\begin{document}

%%% One Hidden MNIST - with first layer params

\begin{preview}
    \begin{tikzpicture}[x=1.5cm, y=1.5cm, >=stealth]
    \inputs{{1,2,missing,784}}
    \hidden{{1,missing,36}}{$\sum \rightarrow \phi$}
    \outputs{{0,missing,9}}{$\sum \rightarrow \phi$}
    \layers
    \hiddenoutputconnections{{bias,1,36}}{{0,9}}

    \foreach \i in {1,2,784}
    \foreach \j in {1}
    \draw [->,red] (input-\i) -- (hidden-\j) node[pos=0.25,left,above] {$\beta^{(1)}_{\j,\i}$};
    \foreach \i in {bias,1,2,784}
    \foreach \j in {36}
    \draw [->,opacity=0.1] (input-\i) -- (hidden-\j);

    \end{tikzpicture}
\end{preview}

\end{document}